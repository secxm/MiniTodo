[project]
name = "minitodo"
version = "0.1.0"
description = "桌面便签式 Todo 代办清单"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pywebview>=5.0",
    "pystray>=0.19.0",
    "pillow>=10.0",
    "nuitka>=2.0",
]
